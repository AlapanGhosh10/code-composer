import './Header.css'

function Header() {
    return (
        <header>
            <p>CodeComposer</p>
            <div id="log-sig">
                <button>Login</button>
                <button>Signup</button>
            </div>
        </header>
    )
}

export default Header;